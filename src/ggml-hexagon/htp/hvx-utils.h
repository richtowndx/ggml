#ifndef HVX_UTILS_H  // 如果未定义 HVX_UTILS_H 则编译
#define HVX_UTILS_H  // 宏定义 HVX_UTILS_H

#include "hex-utils.h"  // 引入 hex-utils.h 头文件

#include "hvx-types.h"  // 引入 hvx-types.h 头文件
#include "hvx-copy.h"  // 引入 hvx-copy.h 头文件
#include "hvx-scale.h"  // 引入 hvx-scale.h 头文件
#include "hvx-exp.h"  // 引入 hvx-exp.h 头文件
#include "hvx-inverse.h"  // 引入 hvx-inverse.h 头文件
#include "hvx-reduce.h"  // 引入 hvx-reduce.h 头文件
#include "hvx-sigmoid.h"  // 引入 hvx-sigmoid.h 头文件
#include "hvx-sqrt.h"  // 引入 hvx-sqrt.h 头文件
#include "hvx-arith.h"  // 引入 hvx-arith.h 头文件
#include "hvx-div.h"  // 引入 hvx-div.h 头文件
#include "hvx-base.h"  // 引入 hvx-base.h 头文件

#endif /* HVX_UTILS_H */  // 条件编译结束
